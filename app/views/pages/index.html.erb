<section class="hero">
  <h1>
    Alle boeken, alle prijzen,<br>
    <span class="wavy-underline">nu alleen nog kiezen.</span>
  </h1>

  <h2>Vind boeken van alle webshops op één plek.<br>Nieuw, tweedehands, geen gedoe.<br>Alle deals direct bij elkaar.</h2>

  <div class="hero__image">
    <%= render "hero_image" %>
  </div>
</section>

<div class="layout">
  <section class="blocks">
    <div class="block">
      <img <%= "data-" unless theme_light? %>src="<%= vite_asset_path("images/doodle-bag-light.png") %>" height="90" width="82" alt="" class="block__image light-only" data-theme="light" loading="lazy" />
      <img <%= "data-" unless theme_dark? %>src="<%=  vite_asset_path("images/doodle-bag-dark.png") %>"  height="90" width="82" alt="" class="block__image dark-only"  data-theme="dark"  loading="lazy" />

      Boeken van meer dan 15 webshop allemaal op 1 plek.
    </div>

    <div class="block">
      <img <%= "data-" unless theme_light? %>src="<%= vite_asset_path("images/doodle-glasses-light.png") %>" height="35" width="73" alt="" class="block__image light-only" data-theme="light" loading="lazy" style="top: 0.5rem" />
      <img <%= "data-" unless theme_dark? %>src="<%=  vite_asset_path("images/doodle-glasses-dark.png") %>"  height="35" width="73" alt="" class="block__image dark-only"  data-theme="dark"  loading="lazy" style="top: 0.5rem" />

      In één simpel overzicht alle prijzen op een rij.
    </div>

    <div class="block">
      <img <%= "data-" unless theme_light? %>src="<%= vite_asset_path("images/doodle-tea-light.png") %>" height="80" width="46" alt="" class="block__image light-only" data-theme="light" loading="lazy" style="left: 0" />
      <img <%= "data-" unless theme_dark? %>src="<%=  vite_asset_path("images/doodle-tea-dark.png") %>"  height="80" width="46" alt="" class="block__image dark-only"  data-theme="dark"  loading="lazy" style="left: 0" />

      Vind gemakkelijk boeken van alle soorten en maten.
    </div>
  </section>

  <section class="mt-8">
    <h2 class="mb-4">Iedereen leest deze.</h2>

    <div class="cards">
      <% cache ["hot-books", @hot_books.maximum(:updated_at)], expires_in: 4.hours do %>
        <%= render collection: @hot_books, partial: "books/card", as: :book %>
      <% end %>
    </div>

    <%= link_to "Bekijk ze allemaal", books_path, class: "button button--large mt-2" %>
  </section>

  <section class="mt-8">
    <h2 class="mb-4">Zin in een andere wereld?</h2>

    <div class="cards">
      <% cache ["hot-fantasy-books", @fantasy_books.maximum(:updated_at)], expires_in: 4.hours do %>
        <%= render collection: @fantasy_books, partial: "books/card", as: :book %>
      <% end %>
    </div>

    <%= link_to "Bekijk alles in fantasy", genre_path("fantasy"), class: "button button--large mt-2" %>
  </section>

  <section class="mt-8">
    <h2 class="mb-4">Misschien iets mysterieus?</h2>

    <div class="cards">
      <% cache ["hot-mystery-books", @mystery_books.maximum(:updated_at)], expires_in: 4.hours do %>
        <%= render collection: @mystery_books, partial: "books/card", as: :book %>
      <% end %>
    </div>

    <%= link_to "Bekijk alles in mysterie", genre_path("mysterie"), class: "button button--large mt-2" %>
  </section>

  <section class="mt-8">
    <h2 class="mb-4">Aan het koken?</h2>

    <div class="cards">
      <% cache ["hot-cook-books", @cook_books.maximum(:updated_at)], expires_in: 4.hours do %>
        <%= render collection: @cook_books, partial: "books/card", as: :book %>
      <% end %>
    </div>

    <%= link_to "Bekijk alle kookboeken", genre_path("kookboeken"), class: "button button--large mt-2" %>
  </section>
</div>
